name: Test Workflow

on:
  push:
    branches:
      - main
      - develop
      - feature/*

jobs:
  migration-validation:
    name: Migration Validation Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run validation
        run: echo "Migration validation successful"
